<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="MovieManagement.Views.User_Setting"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MovieManagement.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >

    <!-- CODE VAO DAY -->
    <ScrollViewer HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
        <StackPanel Margin="32,0,32, 60">
            <TextBlock Text="Change your name" Style ="{StaticResource TitleTextBlockStyle}"/>
            <TextBox Width ="300" Header="Enter your name:" PlaceholderText="Name" />
            <TextBlock Text="Change your password" Style ="{StaticResource TitleTextBlockStyle}" />
            <PasswordBox Width="300" Header="Your recent password" PlaceholderText="*********" PasswordChar="*"  />
            <PasswordBox Width="300" Header="Type new password" PlaceholderText="*********" PasswordChar="*"  />
            <PasswordBox Width="300" Header="Retype new password" PlaceholderText="*********" PasswordChar="*" />
            <Button Content="Save changes" Background="Cyan" HorizontalAlignment ="Right"/>
        </StackPanel>


    </ScrollViewer>
    
</Page>
